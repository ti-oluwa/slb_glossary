[project]
name = "slb-glossary"
version = "0.1.0"
description = "Search the Schlumberger Oilfield Glossary in Python."
readme = "README.md"
license = { text = "BSD-3-Clause" }
authors = [{ name = "Daniel T. Afolayan", email = "tioluwa.dev@gmail.com" }]
requires-python = ">=3.10"
keywords = ["slb", "schlumberger", "glossary", "scraper", "playwright", "patchright", "async"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: BSD License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Internet :: WWW/HTTP :: Indexing/Search",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "patchright>=1.61.0",
    "playwright-stealth>=2.0.3",
    "rich>=13.0.0",
    "click>=8.1.7",
]

[project.optional-dependencies]
xlsx = ["openpyxl>=3.1"]
# Kept for backwards compatibility with `pip install slb-glossary[cli]`;
# click is now a core dependency so the `slb-glossary`/`slb` console
# scripts work out of the box for `pipx install`, `uvx`, and the curl
# installer, none of which let a user opt into an extra first.
cli = []
tui = ["trogon>=0.6.0", "textual>=0.58"]

[project.scripts]
slb-glossary = "slb_glossary.cli.main:main"
slb = "slb_glossary.cli.main:main"

[project.urls]
Homepage = "https://github.com/ti-oluwa/slb_glossary"
Repository = "https://github.com/ti-oluwa/slb_glossary"
Issues = "https://github.com/ti-oluwa/slb_glossary/issues"

[dependency-groups]
dev = ["ruff>=0.13"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["slb_glossary"]

[tool.ruff]
target-version = "py310"
line-length = 99

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "ASYNC", # flake8-async
]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["slb_glossary"]

[tool.ruff.format]
quote-style = "double"
